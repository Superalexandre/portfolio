name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: 🚀 Run deploy
      run: npm run deploy
    - name: Run ESLint
      run: npm run lint
    - name: Run unsued dependencies
      run: npm run depcheck